
-               - cd -
..              - cd ..
...             - cd ...
....            - cd ....
.....           - cd .....
~               - cd ~
c               - cat with beautiful colors.
calc            - Simple calculator.
codepoint       - Get a character’s Unicode code point.
cp_p            - Copy with progress.
dataurl         - Create a data URL from a file.
dnsrecords      - Get all the DNS records.
extract         - Extract archives - use: extract <file>.
fs              - Determine size of a file or total size of a directory.
gita            - git add
gitd            - git diff --ignore-space-change
gitds           - git diff --ignore-space-change --staged
gitfetchpr      - Fetch a pull request to a local branch.
gitignore       - Ignore changes on a tracked file.
gitignored      - List tracked files set to ignore changes.
gitl            - git log --oneline --decorate
gitprune        - Delete all merged branches locally and remotely.
gitpush         - Push the current branch to origin and track it.
gitr            - cd to the root git directory.
gits            - git status
gitunignore     - Stop ignoring changes on a tracked file.
gz              - Compare original and gzipped file size.
httpcompression - Provides the content encoding the specified resources are served with.
httpdump        - View HTTP traffic.
ipexternal      - Get the internet IP address of the network.
iplocal         - List the IP addesses of the computer.
json            - Syntax highlight JSON strings or files.
l               - List all files in long format.
la              - List all files in long format, including dot files.
lsd             - List only directories.
mkd             - Create a new directory and enter it.
nodesecurity    - Checks the current Node.js project's dependencies for known security issues.
npmls           - `npm ls` without all the dependency fluff.
o               - Opens the current directory, otherwise opens the given file, directory or URL.
phpserver       - Start a PHP server from a directory, optionally specifying the port.
s               - Opens the current directory or project in Sublime, otherwise opens the given path.
server          - Start an HTTP server from a directory, optionally specifying the port.
sniff           - View HTTP traffic.
tunnel          - Start the SSH tunnel.
tunnelstop      - Stop the SSH tunnel.
unidecode       - Decode \x{ABCD}-style Unicode escape sequences.
uniencode       - Escape UTF-8 characters into their 3-byte format.
update          - Update installed npm packages and Ruby gems.
urlencode       - URL-encode strings.
